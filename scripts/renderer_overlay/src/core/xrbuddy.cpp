/*
    Headless overlay: empty translation unit. The full XrBuddy implementation
    is inlined in the stub header. This .cpp exists only so the CMakeLists.txt
    sources list (which references xrbuddy.cpp) does not need to change.
*/

#include "xrbuddy.h"
